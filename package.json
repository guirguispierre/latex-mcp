{
  "name": "latex-mcp",
  "version": "1.0.0",
  "description": "MCP server that renders LaTeX math expressions into PNG images",
  "main": "src/index.ts",
  "scripts": {
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "agents": "^0.5.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260302.0",
    "wrangler": "^4.67.0"
  }
}
