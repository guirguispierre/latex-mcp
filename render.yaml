services:
  - type: web
    name: latex-mcp
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: MCP_TRANSPORT
        value: streamable-http
      - key: HOST
        value: 0.0.0.0
      - key: PORT
        value: 8000
    autoDeploy: true
